import { Spectral, Lato } from "next/font/google";

export const spectral = Spectral({
  style: ["normal"],
  subsets: ["latin"],
  weight: ["400"],
});

export const lato = Lato({
  style: ["normal"],
  subsets: ["latin"],
  weight: ["400"],
});
